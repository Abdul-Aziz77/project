const arr = ['a', 'b', 'c']
const arrObj = {
    0: '1',
    'b': '2',
    2: '3'
}
arrObj['v'] = '111'

console.log(arrObj['v'])

const storename = 'Impuls';

const storeDescription = {
    budget: 10000,
    employees: ['Igor', 'Andrew', 'Pavel'],
    products: {
        'phone': 100,
        'pen': 20
    },
    open: true
}